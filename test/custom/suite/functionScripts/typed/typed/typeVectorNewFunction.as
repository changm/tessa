package something {

    class Node {

    };

    function createFunction() {
      var result:Vector.<Number> = new Vector.<Number>();
          var testFunction:Function = function (node:Node):void {
                  //result.push(node.key);
              }
    }

    createFunction();

}
